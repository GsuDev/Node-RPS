{
  "name": "rps-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format:frontend": "cd frontend && npx prettier --write \"src/**/*.ts\"",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "socket.io-client": "^4.6.0"
  }
}